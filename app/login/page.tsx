"use client";
import { Form } from "@/components"

export default function Login() {
    return (
        <main className="h-screen w-full flex justify-center items-center p-5 ">
            <Form />
        </main>
    )
}